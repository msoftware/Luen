<!-- Circular reveal container for the sheet -->
<io.codetail.widget.RevealLinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="end|bottom"
    android:orientation="vertical">

    <!-- Sheet that contains your items -->
    <android.support.v7.widget.CardView
        android:id="@+id/fab_sheet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Recording -->
            <TextView
                android:id="@+id/fab_sheet_item_recording"
                style="@style/TextAppearance.MaterialSheetFab.Sheet.Item"
                android:drawableLeft="@drawable/ic_apps_vector"
                android:drawableStart="@drawable/ic_apps_vector"
                android:text="Open" />

            <!-- Reminder -->
            <TextView
                android:id="@+id/fab_sheet_item_reminder"
                style="@style/TextAppearance.MaterialSheetFab.Sheet.Item"
                android:drawableLeft="@drawable/ic_move_vector"
                android:drawableStart="@drawable/ic_move_vector"
                android:text="Delete" />

            <!-- Photo -->
            <TextView
                android:id="@+id/fab_sheet_item_photo"
                style="@style/TextAppearance.MaterialSheetFab.Sheet.Item"
                android:drawableLeft="@drawable/ic_update_vector"
                android:drawableStart="@drawable/ic_update_vector"
                android:text="Извлечь" />
        </LinearLayout>

    </android.support.v7.widget.CardView>
</io.codetail.widget.RevealLinearLayout>